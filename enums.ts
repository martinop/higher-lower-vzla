
export enum Status {
  IDLE = "idle",
  LOSER = "loser",
  WINNER = "winner"
}

export enum Selection {
  HIGHER = "higher",
  LOWER = "lower",
}

export enum VersusAnimation {
  HIDDEN = "hidden",
  VISIBLE = "visible"
}